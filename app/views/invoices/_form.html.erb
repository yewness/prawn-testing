<%= form_with(model: invoice, local: true) do |form| %>
  <% if invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
      <% invoice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :invoice_no %>
    <%= form.text_field :invoice_no, id: :invoice_invoice_no %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :invoice_name %>
  </div>

  <div class="field">
    <%= form.label :payment_mode %>
    <%= form.text_field :payment_mode, id: :invoice_payment_mode %>
  </div>

  <div class="field">
    <%= form.label :bank_detail %>
    <%= form.text_field :bank_detail, id: :invoice_bank_detail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
